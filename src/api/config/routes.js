/**
 * Containing the API routes for the application.
 */
const apiRoutes = {
  login: "/auth/login/",
  register: "/auth/register/",
  tasks: "/tasks/",
  folders: "/folders/"
};

export default apiRoutes;
